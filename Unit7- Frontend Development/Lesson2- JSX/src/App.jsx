import Menu from "./components/Menu";

// Functional component that serves as the parent component of the app
const App = () => {
  return (
    <div className="app">
      <Menu />
    </div>
  )
};

// Exporting the App component
export default App;



/*

React Component:

Import dependencies

Initialize state for component (optional)

Create a react functional component

Within the body of the component

Write JSX for the actual content you want to render

Export the component 

*/